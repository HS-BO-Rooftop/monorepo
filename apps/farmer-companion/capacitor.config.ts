import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'farmer-companion',
  webDir: '../../dist/apps/farmer-companion',
  bundledWebRuntime: false,
};

export default config;
