import { Component, Input } from '@angular/core';

@Component({
  selector: 'rooftop-login-container',
  template: '',
})
export class MockLoginContainerComponent {
  @Input() backButtonVisible = false;
}
